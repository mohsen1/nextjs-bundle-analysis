# Copyright IBM Corp. 2021, 2025
# SPDX-License-Identifier: MPL-2.0
name: 'Next.js Bundle Analysis'

on:
  pull_request:
  push:
    branches:
      # change this if your default branch is named differently
      - main
  workflow_dispatch:

jobs:
  analyze:
    uses: hashicorp/nextjs-bundle-analysis/.github/workflows/analyze.yml@{PACKAGE_VERSION}
    # with:
    #   node-version: 18
    #   package-manager: npm
    #   working-directory: ./
    #   build-output-directory: .next
    #   build-command: ./node_modules/.bin/next build
    #   output-mode: check
    #   minimum-change-threshold: 1024
    #   minimum-total-change-threshold: 1024
    #   skip-comment-if-empty: true
